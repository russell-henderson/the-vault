import type { Blueprint } from "@the-vault/shared";

type DashboardProps = { blueprints: Blueprint[]; loading: boolean; error?: string; onNavigate: (path: string) => void };

export function Dashboard({ blueprints, loading, error, onNavigate }: DashboardProps) {
  return <><div className="mb-10 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="eyebrow">Workspace</p><h1 className="page-title">Blueprint library</h1><p className="page-subtitle">The durable source of truth between architectural intent and implementation.</p></div><button className="button-primary" onClick={() => onNavigate("/blueprints/new")}>Create blueprint <span>+</span></button></div>{error && <div className="mb-5 alert-error">{error}</div>}{loading ? <div className="empty-panel"><div className="spinner" /><p>Loading your blueprints…</p></div> : blueprints.length === 0 ? <div className="empty-panel min-h-[360px] flex-col justify-center text-center"><span className="mb-5 grid h-14 w-14 place-items-center rounded-2xl bg-cyan-400/10 text-2xl text-cyan-300">⌘</span><h2 className="text-xl font-semibold text-white">Your architecture vault is empty</h2><p className="mt-2 max-w-md text-sm text-slate-400">Start with a component blueprint. Capture the context once, then turn it into a Codex-ready implementation handoff.</p><button className="button-primary mt-6" onClick={() => onNavigate("/blueprints/new")}>Create your first blueprint</button></div> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{blueprints.map((blueprint) => <button className="panel card-hover text-left" key={blueprint.id} onClick={() => onNavigate(`/blueprints/${blueprint.id}`)}><div className="flex items-start justify-between gap-3"><span className="tag">{blueprint.framework}</span><span className="text-xs text-slate-500">{new Date(blueprint.updatedAt).toLocaleDateString()}</span></div><h2 className="mt-5 text-lg font-semibold text-white">{blueprint.name}</h2><p className="mt-2 line-clamp-3 text-sm leading-6 text-slate-400">{blueprint.description}</p><div className="mt-6 flex items-center gap-4 text-xs text-slate-500"><span>{blueprint.language}</span><span>·</span><span>{blueprint.dependencies.length} dependencies</span></div></button>)}</div>}</>;
}
